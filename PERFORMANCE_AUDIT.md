# تقرير التدقيق الفني وتحسين الأداء | Performance Audit Report
**الدور:** Senior React + Supabase Performance Engineer
**الهدف:** تشغيل النظام بأعلى كفاءة واستقرار على الخطة المجانية لـ Supabase (Free Plan)، وتقليل استهلاك الموارد وحجم البيانات المنقولة (Egress) مع ضمان سلامة البيانات المالية 100%.

---

## 📄 جدول المحتويات
1. [المشاكل التي تم رصدها (Critical & High)](#1-المشاكل-التي-تم-رصدها)
2. [ما تم إصلاحه وتطويره بالكامل](#2-ما-تم-إصلاحه-وتطويره-بالكامل)
3. [استراتيجية وقوانين الفهرسة المقترحة (Supabase Indexes)](#3-استراتيجية-وقوانين-الفهرسة-المقترحة)
4. [استراتيجية تحسين الاستعلامات والحد من استهلاك الـ Egress](#4-استراتيجية-تحسين-الاستعلامات-والحد-من-استهلاك-الـ-egress)
5. [استراتيجية الـ Pagination (تقسيم البيانات على الخادم)](#5-استراتيجية-الـ-pagination)
6. [استراتيجية التخزين المؤقت للبيانات (Caching & TanStack Query)](#6-استراتيجية-التخزين-المؤقت)
7. [سلامة وصحة العمليات المالية (Financial Data Safety)](#7-سلامة-وصحة-العمليات-المالية)
8. [توصيات للمراقبة والمستقبل](#8-توصيات-للمراقبة-والمستقبل)

---

## 1. المشاكل التي تم رصدها

### 🔴 مشاكل حرجة (Critical / High)
* **البحث الفوري المتكرر على كل ضغطة زر (Keystroke Recalculation):**
  في الواجهات الأساسية (مركز العمليات السريع، إدارة المكاتب الخارجية، الموزعين، البحث عن العملاء، وسجلات التدقيق)، كان تصفية البيانات في React تعيد العمليات الحسابية والفلترة عند كل حرف يكتبه المستخدم في حقل البحث. مع تزايد عدد السجلات، يسبب ذلك بطئًا ملحوظًا في استجابة المتصفح وضغطًا مضاعفًا على معالج الجهاز، وإذا رُبط مباشرة بالخلفية فسيؤدي لاستنفاد الحد الأقصى للاستعلامات في Supabase Free Plan خلال ساعات معدودة.
* **الاعتماد الكلي على جلب كامل البيانات للفلترة (Fetch All -> Filter in Browser):**
  مئات السجلات لعمليات السجلات والعملاء كانت تُقرأ دفعة واحدة، وهو ما يشكل خطرًا داهمًا على استهلاك سعة نقل البيانات (Egress) المقدرة بـ 2GB فقط في خطة Supabase المجانية.

### 🟡 مشاكل متوسطة (Medium)
* **غياب الفهرسة المثالية للجداول المقترحة:**
  مع نمو حجم العمليات (Orders)، والمدفوعات (Payments)، وسجل الحركات المالية (Ledger)، ستعاني قواعد البيانات من استعلامات بطيئة (Full Table Scans) بسبب عدم تحديد فهارس (Indexes) مخصصة للحقول الحساسة.
* **احتمالية تكرار الطلبات (Duplicate Requests / Re-renders):**
  وجود عمليات مالية كإنشاء المعاملات وتسجيل الدفعات يتطلب حماية صارمة لمنع تكرار الضغط (Double Click) وضمان الـ Idempotency لمنع تكرار القيود.

---

## 2. ما تم إصلاحه وتطويره بالكامل

### 🛡️ إدراج نظام التأخير الذكي (Search Debouncing Engine)
تم إنشاء هوك مخصص ومستقر باسم `useDebounce` لتجنب معالجة الفلترة بشكل عشوائي مع كل حرف مكتوب:
* **موقع الكود:** `/src/hooks/useDebounce.ts`
* **آلية العمل:** يقوم بتأخير تحديث نص البحث بمقدار `300ms` حتى ينتهي المستخدم من الكتابة تماماً.
* **الواجهات المحدثة:**
  1. **مركز العمليات السريع (`OperationsCenter.tsx`):** تم دمج الهوك لتصفية المعاملات والباركود وأرقام الهواتف بشكل سلس.
  2. **شاشة المكاتب الخارجية (`ExternalOfficesView.tsx`):** دمج الهوك لتصفية وعرض المكاتب وعناوينها فقط بعد توقف الكتابة.
  3. **شاشة سجل التدقيق والعمليات (`AuditLogsView.tsx`):** وهي الأكثر ثقلاً من حيث البيانات، تم تحديثها بالهوك لفلترة السجلات بذكاء.
  4. **إدارة الموزعين وحساباتهم (`DistributorsView.tsx`):** تصفية الموزعين والرموز الهاتفية بكفاءة عالية.
  5. **نموذج تسجيل المعاملات الجديدة (`NewServiceOrder.tsx`):** تم دمج الهوك في حقل البحث الفوري عن العملاء المسجلين مسبقاً لمنع المعالجة العشوائية.

---

## 3. استراتيجية وقوانين الفهرسة المقترحة (Supabase Indexes)

لتشغيل الاستعلامات بلمح البصر على الـ Database وتقليل وقت المعالجة (CPU Time)، يجب تطبيق الفهارس التالية فور تفعيل قاعدة البيانات السحابية Supabase:

```sql
-- 1. فهرس للعمليات المالية والمعاملات (تصفية حسب الفرع وتاريخ الإنشاء)
CREATE INDEX IF NOT EXISTS idx_orders_branch_created 
ON orders (current_branch_id, created_at DESC);

-- 2. فهرس للبحث السريع عن العملاء بالهاتف والاسم (يمنع Full Table Scan)
CREATE INDEX IF NOT EXISTS idx_customers_search 
ON customers (phone, name);

-- 3. فهرس لربط القيود المالية والمقبوضات بالمعاملات والفرع
CREATE INDEX IF NOT EXISTS idx_ledger_branch_created 
ON ledger (branch_id, created_at DESC);

-- 4. فهرس لربط الدفعات بالمعاملات لسرعة جلب كشف حساب المعاملة
CREATE INDEX IF NOT EXISTS idx_payments_order 
ON payments (order_id);

-- 5. فهرس لسجلات الرقابة والتدقيق (تنمو بسرعة فائقة)
CREATE INDEX IF NOT EXISTS idx_audit_logs_created 
ON audit_logs (created_at DESC);

-- 6. فهرس للموزعين والمكاتب الخارجية لحساب الأرصدة بسرعة
CREATE INDEX IF NOT EXISTS idx_distributors_active ON distributors (id) WHERE is_active = true;
CREATE INDEX IF NOT EXISTS idx_external_offices_active ON external_offices (id) WHERE is_active = true;
```

* **السبب والجدوى:** تمنع هذه الفهارس استهلاك موارد المعالج في الخطة المجانية وتضمن استرجاع نتائج البحث في أجزاء من الميلي ثانية حتى مع وجود عشرات الآلاف من السجلات.

---

## 4. استراتيجية تحسين الاستعلامات والحد من استهلاك الـ Egress

لمنع تجاوز حد الـ Egress (نقل البيانات) المجاني البالغ 2GB، نلزم المطورين مستقبلاً باتباع قواعد الاستعلام الصارمة التالية عند ربط تطبيق الـ React بـ Supabase:

### ❌ النمط السيئ (يهدر سعة نقل البيانات):
```typescript
// يجلب كل البيانات والأعمدة والعلاقات الكبيرة لكل سطر حتى لو لم تكن ظاهرة بالجدول
const { data } = await supabase.from('orders').select('*');
```

###  النمط الاحترافي المحسن (صديق الخطة المجانية):
```typescript
// جلب الأعمدة المعروضة في الجدول فقط وتحديد عدد النتائج بـ 20 سطرًا
const { data } = await supabase
  .from('orders')
  .select('id, order_number, customer_name, status, total, remaining, created_at')
  .order('created_at', { ascending: false })
  .range(0, 19);
```

---

## 5. استراتيجية الـ Pagination (تقسيم البيانات على الخادم)

يجب تطبيق هذه الاستراتيجية في كل القوائم الديناميكية التي يزداد حجمها مع الوقت (المعاملات، سجل التدقيق، الحركات المالية، المصروفات):

1. **التحميل على دفعات (Offset-based Pagination):**
   * عرض 15-20 سجلاً فقط كحد أقصى في كل صفحة.
   * إرسال متغيرات `page` و `limit` إلى دالة الاستعلام وتحويلها برمجياً إلى نطاق `.range(start, end)` في استعلام Supabase:
     ```typescript
     const start = page * limit;
     const end = start + limit - 1;
     const { data, count } = await supabase
       .from('orders')
       .select('...', { count: 'exact' })
       .range(start, end);
     ```
2. **منع التحميل المسبق (No Eager Loading):**
   * لا يتم تحميل تفاصيل المعاملة الإضافية (مثل الحقول المخصصة الكاملة وتواريخ التعديل وجميع سجلات المراجعة) في جدول العرض الرئيسي.
   * تُحمل تفاصيل المعاملة **فقط** عندما يضغط الموظف على زر "عرض التفاصيل" لفتح نافذة الفحص الخاصة بالمعاملة المحددة.

---

## 6. استراتيجية التخزين المؤقت للبيانات (Caching & TanStack Query)

عند ربط التطبيق بـ TanStack Query، يجب تقسيم البيانات إلى فئتين:

### أ. البيانات شبه الثابتة (Static / Lookup Data)
* **أمثلة:** قائمة الخدمات المتاحة، الفروع، الموزعين النشطين، تصنيفات المصروفات.
* **إعدادات الكاش:**
  * `staleTime: 1000 * 60 * 30` (30 دقيقة كاملة).
  * `gcTime: 1000 * 60 * 60` (ساعة كاملة).
* **الفائدة:** يمنع التطبيق من تكرار الاستعلام عن هذه القوائم كلما تنقل الموظف بين شاشات التطبيق، مما يخفض عدد الـ Requests بنسبة تصل إلى **80%**.

### ب. البيانات الديناميكية والحساسة ماليًا (Dynamic / Financial Data)
* **أمثلة:** رصيد الخزينة، سجل القيود المالية، دفعات المعاملة، التوريدات والتسويات.
* **إعدادات الكاش:**
  * `staleTime: 0` (تعتبر قديمة فوراً ويجب التأكد من الخادم عند الحاجة).
  * الاعتماد على الإبطال اليدوي المباشر للكاش (`Query Invalidation`) بمجرد نجاح أي عملية كتابة (Mutation) متمثلة في دفع مبلغ، توريد نقدي، تحويل فروع، أو تسجيل مصروف.

---

## 7. سلامة وصحة العمليات المالية (Financial Data Safety)

تحسين الأداء يجب ألا يمس بأي حال من الأحوال سلامة ودقة الحسابات، ولذلك نؤكد على القواعد التالية:

1. **العمليات المالية غير قابلة للتخزين المؤقت المشكوك فيه (No Dirty Cash Cache):**
   * يتم قراءة رصيد الدرج الفعلي (الخزينة) عن طريق الخادم دائماً بعملية تجميعية (Aggregate) مباشرة، دون الاعتماد على كاش محلي قد يخفي عجزاً أو تكراراً في الأموال.
2. **تأكيد مفاتيح تفادي التكرار (Idempotency Safeguard):**
   * يحتوي التطبيق بالفعل على نظام فريد لتوليد واستخدام مفاتيح تفادي تكرار الطلبات `Idempotency Keys`.
   * يجب تمرير هذا المفتاح لكل عملية مالية (دفع، مصروف، توريد) للتأكد في جانب قاعدة البيانات أنه لن يتم معالجة أو تكرار القيد المالي في حال نقر الموظف على الزر مرتين متتاليتين بسرعة.
3. **دفتر الحسابات التراكمي المقيد (Append-Only Ledger):**
   * جميع حركات الأموال في الخزينة تسجل كأسطر لا تقبل التعديل أو الحذف، والتسويات تتم حصراً بإضافة قيود جديدة متعاكسة للحفاظ على نزاهة التدقيق المحاسبي.

---

## 8. توصيات للمراقبة والمستقبل

* **تجنب تفعيل البث الفوري المفتوح (Realtime Channel):**
  الخطة المجانية من Supabase تحدد عدد الاتصالات المتزامنة للبث الفوري بـ 200 اتصال فقط. يوصى بعدم تفعيل خاصية Realtime لجميع جداول النظام، والاعتماد بدلاً من ذلك على استدعاءات عادية أو تفعيل القنوات فقط لجدول "التحويلات بين الفروع" بشكل ضيق ولفروع محددة لمنع استهلاك سعة البث المجانية.
* **مراقبة نمو سجلات التدقيق (Audit Logs):**
  سجلات العمليات تنمو بسرعة صاروخية وتستهلك مساحة التخزين (Database Space - حد 500MB للخطة المجانية). يوصى بإنشاء نظام تفريغ تلقائي (Auto-archiving) أو تقليص مساحة تفاصيل الـ metadata المخزنة بالسجل والاكتفاء برؤوس العناوين.
