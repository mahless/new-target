# معايير الأمان وحماية العمليات | Security & Concurrency

## 1. منع التكرار وإدارة التزامن (Idempotency & Concurrency)

1. **Idempotency Keys**:
   - كل طلب إنشاء معاملة، أو تحصيل دفعة، أو تسجيل مصروف، أو تحويل فروع يرسل مفتاحاً فريداً `idempotency_key`.
   - يتم التحقق من وجود المفتاح في السجلات قبل إتمام أي قيد مالي.
   - في حال تكرار النقر أو إعادة إرسال الطلب بعد انقطاع الاتصال، يعيد النظام استجابة المعاملة الأولى دون إنشاء تكرار مالي.

2. **قفل واجهة المستخدم (UI Action Locking)**:
   - تعطيل أزرار الحفظ فور الضغط الأول وعرض مؤشر المعالجة.
   - حماية ضد النقر المزدوج (Double-Click Protection) مع إلغاء الطلبات المتكررة تلقائياً.

3. **حماية الصلاحيات المستقبلية (Future RBAC / RLS)**:
   - الهيكل مهيأ لربط Supabase Row Level Security وسياسات العزل بين أدوار المديرين والموظفين والمشاهدين.
   - عدم استخدام مفاتيح Service Role في واجهة العميل.
