# المنطق المالي والمعادلات المحاسبية | Financial Logic & Ledger

## 1. معادلات الخدمة والتحصيل (Order & Payment Formulas)

```
إجمالي المدفوع (Total Paid) = الكاش (Cash Amount) + الإلكتروني (Electronic Amount)
المتبقي (Remaining) = سعر الخدمة (Service Price) - إجمالي المدفوع (Total Paid)
```

- **قيد المبالغ**: لا يُسمح بإدخال مبالغ سالبة (`amount >= 0`).
- **تجاوز السعر**: لا يُسمح بأن يتجاوز إجمالي المدفوع سعر الخدمة (`Total Paid <= Service Price`).

---

## 2. قواعد حركة الدرج النقدي للفرع (Branch Cash Drawer Rules)

الدرج النقدي يتأثر **فقط بالنقد الحقيقي**، ولا يتأثر بالدفع الإلكتروني (InstaPay / المحافظ).

### الحركات الداخلة (Cash In / +):
1. `customer_cash_payment`: نقد تم تحصيله من العميل كدفعة أولى أو دفعة متبقية.
2. `distributor_payment`: نقد تم توريده من الموزع لتسوية حسابه.
3. `branch_transfer_in`: نقد محول من فرع آخر.
4. `opening_balance`: الرصيد الافتتاحي للتشغيل.

### الحركات الخارجة (Cash Out / -):
1. `expense`: مصروف تشغيلي تم دفعه من الدرج.
2. `external_office_cost`: تكلفة تنفيذ المعاملة المدفوعة للمكتب الخارجي.
3. `branch_transfer_out`: نقد محول إلى فرع آخر.
4. `daily_closing_payout`: ترحيل رصيد الإغلاق اليومي للبدء برصيد تشغيلي جديد.

---

## 3. حساب هامش المكتب الخارجي (External Office Margin)

عند اختيار مكتب خارجي لتنفيذ المعاملة:
```
تكلفة الخدمة للعميل (Customer Price) = 1,000 ج.م
تكلفة المكتب الخارجي (External Office Cost) = 900 ج.م
هامش ربح المكتب (Office Gross Margin) = 1,000 - 900 = 100 ج.م
```
- تسجل الـ 900 ج.م كمصروف مباشر مرتبط بالمعاملة يخصم من كاش الفرع فور الصرف.

---

## 4. حسابات الموزعين (Distributor Accounts)

- عند إسناد معاملة لموزع بقيمة (X)، تُضاف القيمة إلى رصيد المستحقات على الموزع.
- عند توريد الموزع مبلغاً نقدياً (Y):
  - يتم قيد حركة توريد نقدي تزيد درج الفرع الذي استلم التوريد.
  - يُخصم المبلغ من رصيد الموزع المستحق:
    `المتبقي على الموزع = إجمالي خدمات الموزع - إجمالي التوريدات النقدية`
